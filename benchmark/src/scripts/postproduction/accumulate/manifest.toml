# Manifest for post-production analysis
# Lists W&B runs to download and aggregate for analysis

# Run names to download
# These are the run names shown in W&B dashboard
# The tool will look up the run ID automatically
run_names = [
    "2025-11-20T07-48-32__control-functional__idx0-2",
    "2025-11-20T07-45-23__control-functional__s0__n2",
]

# Project configuration
[project]
entity = "fvspec"
project = "fvspec"

# Local storage path (relative to benchmark/ root)
# Uses artifacts/ which is already gitignored
output_dir = "dataset"
