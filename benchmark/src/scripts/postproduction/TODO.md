# postproduction todo

1. [ ] recover faithfulness and interestingness. in the grader.
    a. check first if `qa.json` has them and it was just an issue of the `merge` script dropping them. 
2. [ ] mark `fut_autoform` for the ones that actually implemented the functions under test and didn't stub them out as sorry (maybe in `merge/extra_metadata.py`?)
3. [ ] Put `realpbt` in the names of all `realpbt`-derived fields, like `repo_id`=>`realpbt_repo_id` (do this inside `prune.py`)

