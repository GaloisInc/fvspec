# Autoformalization agents

In the nomenclature of this repo, a `unit` test is an `example` test. I.e., a single point in the type, as opposed to `pbt` or `spec` which is a constraint to procedurally generate examples.
