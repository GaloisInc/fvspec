{% if is_function_under_test %}{% include 'impl/common/fragments/function_under_test.prompt.template' %}

{% endif %}{% if "pbt_function" in tags %}You are given a function name `{{ dep_name }}` that appears in a property-based test.
Provide a Lean 4 implementation (using mvcgen/imperative style) by inferring its behavior from the test context below.

**Function to implement**: `{{ dep_name }}`

**Test context** (infer behavior from this):
```python
{{ python_source }}
```

**Your task**:
1. Analyze how `{{ dep_name }}` is used in the test
2. Infer its signature and behavior from the test assertions
3. Implement it in Lean 4 using mvcgen-style (`do` notation, Hoare triples if needed)
4. For stdlib functions, use appropriate Lean libraries or provide implementation
5. Ensure your implementation matches the test's expectations

{% else %}You are given metadata for a stateful Python helper. Translate it into Lean 4 code that uses mvcgen-style reasoning to keep the computation executable and verifiable.

Dependency metadata:
- Qualified Python name: {{ dep_name }}
{% if module_path %}- Python module path: {{ module_path }}{% endif %}
- Callable kind: {{ callable_kind }}

{% if callable_signature %}
Python signature:
```python
{{ callable_signature }}
```
{% endif %}
{% if python_docstring %}
Docstring:
"""
{{ python_docstring }}
"""
{% endif %}
{% if callable_arguments %}
Arguments:
{% for arg in callable_arguments %}
- {{ arg.name }} ({{ arg.role }}){% if arg.annotation %}: {{ arg.annotation }}{% endif %}{% if arg.default %} = {{ arg.default }}{% endif %}
{% endfor %}
{% endif %}
{% if tags %}
Tags: {{ tags | join(", ") }}
{% endif %}
{% if usage_example %}
Usage example:
```python
{{ usage_example }}
```
{% endif %}

Python source:
```python
{{ python_source }}
```
{% endif %}

Normalization plan:
{% if normalization_strategy == "as_is" %}
- Implement the helper as a standalone procedure `{{ lean_helper_name }}` mirroring the Python definition `{{ normalization.function_basename }}`.
{% elif normalization_strategy == "flatten_method" %}
- Model the receiver parameter `{{ normalization.receiver_name }}` explicitly (use `{{ normalization.receiver_alias }}` in Lean) and expose an imperative helper `{{ normalization.lean_helper_name }}` that replaces the method `{{ normalization.function_basename }}`.
{% elif normalization_strategy == "structure_method" %}
- Introduce a Lean structure `{{ normalization.struct_name }}` with observable fields {{ normalization.struct_fields | join(", ") if normalization.struct_fields else "(none discovered)" }} to capture receiver state.
- Implement `{{ normalization.lean_helper_name }}` so it operates on an instance (`{{ normalization.receiver_alias }}`){% if normalization.mutates_receiver %} and returns the updated structure and result{% else %} while leaving the instance unchanged{% endif %}, supported by mvcgen Hoare triples.
{% endif %}
{% if normalization_notes %}- Notes: {{ normalization_notes }}{% endif %}

Guidelines:
1. Ensure the resulting definitions reside in the module `{{ lean_module_qualified }}` (file stem `{{ lean_file_stem }}.lean`). **Do NOT include a `namespace` block in this module; the aggregation logic will wrap all dependencies in a single namespace block in the aggregated `Impl.lean` file. Write all definitions as if they are already inside the correct namespace.**
2. Model imperative structure with `do` notation, mutable references, or arrays as needed. When loops or mutation occur, accompany them with mvcgen-style Hoare triples (`⦃ P ⦄ program ⦃ Q ⦄`) and call `vcg`/`simp` to discharge verification conditions.
3. Provide an executable demonstration (e.g. `#eval`, `#reduce`, or an explicit theorem) showing the helper reduces to the expected behaviour on a representative input.
4. {% include 'common/fragments/computable_requirements.prompt' %} **Do NOT use `sorry`**; dependencies must be fully implemented.
5. Focus solely on the dependency implementation and its minimal verification. Do **not** emit property-based tests or unrelated benchmark code.

{% include 'common/fragments/output_format.prompt' %}
