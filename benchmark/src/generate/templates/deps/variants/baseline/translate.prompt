You are given a Python dependency snippet extracted from a property-based test.
Translate it into Lean 4 so downstream provers can compute with it.

Metadata:
- Dependency name: {{ dep_name }}
- Source hash: {{ source_hash }}
{% if tags %}- Tags: {{ tags | join(", ") }}{% endif %}
{% if usage_example %}
- Usage context:
```
{{ usage_example }}
```
{% endif %}

Python source:
```python
{{ python_source }}
```

Requirements:
1. Emit Lean code for the module `Fvspec.Deps.{{ dep_module }}`.
2. Include a small demonstration that the code reduces (`#eval`, `simp`, or lemma).
3. Avoid axioms. If you must use `sorry`, add a TODO comment explaining what proof or computation is missing.
4. Use Mathlib primitives when they provide the same behaviour.
5. Return only Lean code inside `<code>...</code>` tags so tools can extract it.
