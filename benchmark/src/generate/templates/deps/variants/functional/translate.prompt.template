You are given metadata describing a Python helper that property-based tests depend on.
Translate it into Lean 4 while preserving the observable behaviour.

Dependency metadata:
- Qualified Python name: {{ dep_name }}
{% if module_path %}- Python module path: {{ module_path }}{% endif %}
- Callable kind: {{ callable_kind }}

{% if callable_signature %}
Python signature:
```python
{{ callable_signature }}
```
{% endif %}
{% if python_docstring %}
Docstring:
"""
{{ python_docstring }}
"""
{% endif %}
{% if callable_arguments %}
Arguments:
{% for arg in callable_arguments %}
- {{ arg.name }} ({{ arg.role }}){% if arg.annotation %}: {{ arg.annotation }}{% endif %}{% if arg.default %} = {{ arg.default }}{% endif %}
{% endfor %}
{% endif %}
{% if tags %}
Tags: {{ tags | join(", ") }}
{% endif %}
{% if usage_example %}
Usage example:
```python
{{ usage_example }}
```
{% endif %}

Python source:
```python
{{ python_source }}
```

Normalization plan:
{% if normalization_strategy == "as_is" %}
- Implement the helper as a pure function named `{{ lean_helper_name }}` that mirrors the Python definition `{{ normalization.function_basename }}`.
{% elif normalization_strategy == "flatten_method" %}
- Treat the receiver parameter `{{ normalization.receiver_name }}` as an explicit argument (use `{{ normalization.receiver_alias }}` in Lean) and expose a helper function `{{ normalization.lean_helper_name }}` that replaces the method `{{ normalization.function_basename }}`.
{% elif normalization_strategy == "structure_method" %}
- Define a Lean structure `{{ normalization.struct_name }}` capturing the receiver state (fields: {{ normalization.struct_fields | join(", ") if normalization.struct_fields else "none discovered" }}).
- Implement a helper `{{ normalization.lean_helper_name }}` that takes an instance (`{{ normalization.receiver_alias }}`) and returns its result{% if normalization.mutates_receiver %}, along with the updated structure{% endif %}.
{% endif %}
{% if normalization_notes %}- Notes: {{ normalization_notes }}{% endif %}

Guidelines:
1. Ensure the resulting definitions reside in the module `{{ lean_module_qualified }}` (file stem `{{ lean_file_stem }}.lean`). **Do NOT include a `namespace` block in this module; the aggregation logic will wrap all dependencies in a single namespace block in the aggregated `Deps.lean` file. Write all definitions as if they are already inside the correct namespace.**
2. Produce transparent, pure definitions that compute—prefer recursion, pattern matching, and Batteries helpers over axioms or opaque stubs.
3. Include at least one small evaluation (`#eval`, `#reduce`, or a decidable lemma) that demonstrates the Lean code reduces to the expected result.
4. Minimise `sorry`; when unavoidable, leave a `-- TODO:` comment that explains the missing reasoning.
5. Generate only the dependency implementation and any minimal support it requires. Do **not** emit property-based tests, benchmark specs, or unrelated scaffolding.

## Output Format
When you have finalized your implementation, respond with Lean 4 code wrapped in `<code>...</code>` tags.

Example format: <code>def helper : Nat := 42</code>

Use LSP tools to iteratively check and refine your code before providing the final `<code>...</code>` response.
