{% if deps|length > 0 %}**First**: Call `autoformalize_dependency_tool` for each dependency below:

<dependencies>
{% for dep in deps %}
{{ dep }}
{% endfor %}
</dependencies>

{% endif %}Property-based test:
<theorem>
{{ pbt }}
</theorem>

{% if deps|length > 0 %}After formalizing dependencies, translate to Lean 4. Use formalized deps from `Fvspec.Deps`. Use `sorry` placeholders.
{% else %}Translate to Lean 4. Include function signatures for the test target. Use `sorry` placeholders.
{% endif %}
