{% if deps|length > 0 %}**CRITICAL FIRST STEP**: The following Python dependencies must be formalized before writing your specification:

<dependencies>
{% for dep in deps %}
{{ dep }}
{% endfor %}
</dependencies>

**You MUST do this FIRST**:
1. Call `autoformalize_dependency_tool` once for each dependency above
2. Pass the exact Python source shown for each dependency
3. Wait for all dependency formalizations to complete
4. ONLY THEN write your Lean 4 specification

{% endif %}Translate the following property-based test into Lean 4:

<theorem>
{{ pbt }}
</theorem>
{% if deps|length > 0 %}

After dependencies are formalized, they will be available as `Fvspec.Deps.<module>.<function>` for use in your specification.
{% endif %}
