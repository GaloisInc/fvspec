Translate the following property-based test into Lean 4 theorem statements:

<test>
{{ pbt_code }}
</test>

## Function Under Test
Primary function: `{{ function_name }}`

{% if impl_signatures %}
## Available Function Signatures
The following functions are already implemented in `Fvspec.Impl`:
{% for name, sig in impl_signatures.items() %}
- `{{ sig }}`
{% endfor %}

Use `import Fvspec.Impl` to access these implementations in your specification.
{% endif %}

## Task
Generate Lean 4 theorem statements that capture the properties tested in the Python code:
1. Write theorem statements with `sorry` placeholders for proofs (we're declaring, not proving!)
2. Use the function signatures from Impl.lean
3. Use `Fvspec.Spec` namespace
4. Focus on capturing the test's invariants, not implementation details

Output your specification in <code>...</code> tags.
