# Claude 4.6 experiments

In this dir, write a script `run.sh` that I can run from `./benchmark`, so the bash command will be `./operations/46/run.sh`, where:
1. fix a ranseed (how about 0)
2. grab 500 samples
3. in one tmux window, run the generation pipeline with claude-opus-4-6. In another tmux window, run the generation pipeline on the same 500 samples with claude-sonnet-4-6

do not run the script. i will run it. 
