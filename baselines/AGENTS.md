# Baselines

This directory contains baseline implementations for measuring fvspec benchmark performance.

## Structure

Currently minimal. This area will contain:
- Baseline model implementations
- Evaluation scripts for measuring baseline performance
- Comparison utilities

## Development

```bash
# From ./baselines directory
uv sync  # Install dependencies
```

See root `AGENTS.md` for general development guidelines.

## Future Work

Baseline implementations will be added to establish performance floors and validate benchmark difficulty.
